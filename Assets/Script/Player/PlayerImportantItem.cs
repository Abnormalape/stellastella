using System;

class PlayerImportantItem
    // 중요아이템을 가지고 있는지 아닌지에 대한 판정
    // 최초로 true로 전환될때 빼고는 호출만 가능
{
    bool skullKey = false;
    bool dwarfLanguage = false;
    bool bags1 = false;
    bool bags2 = false;
    int starDrop = 0;
}